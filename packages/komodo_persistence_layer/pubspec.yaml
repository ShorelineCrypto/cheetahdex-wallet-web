name: komodo_persistence_layer
description: Persistence layer abstractions for Flutter/Dart.
version: 0.0.1
publish_to: none

resolution: workspace

environment:
  sdk: ">=3.8.1 <4.0.0"

# Add regular dependencies here.
dependencies:
  # Approved via https://github.com/GLEECBTC/gleec-wallet/pull/1106
  hive_ce: ^2.19.3 # Community-maintained Hive package with WASM support

dev_dependencies:
  lints: ^5.1.1
  test: ^1.30.0
